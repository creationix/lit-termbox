include(GenerateExportHeader)
cmake_minimum_required (VERSION 2.6)
project (termbox)
include_directories(termbox/src)
add_library(termbox SHARED termbox/src/termbox.c termbox/src/utf8.c)
target_link_libraries(termbox)
